appId: chat.gratonite.mobile
---
# Open first guild and verify channel list renders
- assertVisible: "Portals"
- tapOn:
    id: "guild-item"
    index: 0
# Channel list should render inside the drawer
- assertVisible:
    id: "channel-item"
    index: 0
    timeout: 5000
